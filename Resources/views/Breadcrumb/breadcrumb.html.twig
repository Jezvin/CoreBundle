<ol class="breadcrumb">
    {% for item in breadcrumb %}
        {% if loop.last %}
            <li class="active">
                {% if loop.first %}
                    <i class="fa fa-dashboard"></i>&nbsp;
                {% endif %}
                {{ (breadcrumb.translationPrefix ~ item.label) | trans }}
            </li>
        {% else %}
            <li>
                <a href="{{ item.url }}">
                    {% if loop.first %}
                        <i class="fa fa-dashboard"></i>
                    {% endif %}
                    {{ (breadcrumb.translationPrefix ~ item.label) | trans }}
                </a>
            </li>
        {% endif %}
    {% endfor %}
</ol>